import { DersfilterPipe } from './dersfilter.pipe';

describe('PostfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DersfilterPipe();
    expect(pipe).toBeTruthy();
  });
});